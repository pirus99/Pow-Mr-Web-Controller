/* app.js – shared utilities (page-specific JS lives in templates) */

/**
 * Escape HTML special characters.
 */
function escHtml(s) {
  return String(s)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}
